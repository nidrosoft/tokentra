import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Costs by Project - TokenTRA",
  description: "View costs breakdown by project",
};

export default function CostsByProjectPage() {
  return (
    <div>
      <h1>Costs by Project</h1>
    </div>
  );
}
