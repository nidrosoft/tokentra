import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Costs by Provider - TokenTRA",
  description: "View costs breakdown by AI provider",
};

export default function CostsByProviderPage() {
  return (
    <div>
      <h1>Costs by Provider</h1>
    </div>
  );
}
