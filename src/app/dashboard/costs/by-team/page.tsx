import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Costs by Team - TokenTRA",
  description: "View costs breakdown by team",
};

export default function CostsByTeamPage() {
  return (
    <div>
      <h1>Costs by Team</h1>
    </div>
  );
}
