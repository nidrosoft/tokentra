import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Costs by Feature - TokenTRA",
  description: "View costs breakdown by feature tag",
};

export default function CostsByFeaturePage() {
  return (
    <div>
      <h1>Costs by Feature</h1>
    </div>
  );
}
