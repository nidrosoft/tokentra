import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Costs by Model - TokenTRA",
  description: "View costs breakdown by AI model",
};

export default function CostsByModelPage() {
  return (
    <div>
      <h1>Costs by Model</h1>
    </div>
  );
}
