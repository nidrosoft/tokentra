import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Project - TokenTRA",
  description: "Create a new project",
};

export default function CreateProjectPage() {
  return (
    <div>
      <h1>Create Project</h1>
    </div>
  );
}
