import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "API Keys - TokenTRA",
  description: "Manage your API keys",
};

export default function ApiKeysPage() {
  return (
    <div>
      <h1>API Keys</h1>
    </div>
  );
}
