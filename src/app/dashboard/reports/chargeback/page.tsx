import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Chargeback Reports - TokenTRA",
  description: "Generate chargeback reports for cost allocation",
};

export default function ChargebackPage() {
  return (
    <div>
      <h1>Chargeback Reports</h1>
    </div>
  );
}
