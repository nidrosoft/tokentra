import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Export Data - TokenTRA",
  description: "Export your cost and usage data",
};

export default function ExportPage() {
  return (
    <div>
      <h1>Export Data</h1>
    </div>
  );
}
