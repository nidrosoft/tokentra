import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Connect Provider - TokenTRA",
  description: "Connect a new AI provider",
};

export default function ConnectProviderPage() {
  return (
    <div>
      <h1>Connect Provider</h1>
    </div>
  );
}
