import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Model Routing - TokenTRA",
  description: "Configure smart model routing rules",
};

export default function ModelRoutingPage() {
  return (
    <div>
      <h1>Model Routing</h1>
    </div>
  );
}
