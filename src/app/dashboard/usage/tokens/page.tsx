import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Token Usage - TokenTRA",
  description: "Analyze token consumption across providers",
};

export default function TokenUsagePage() {
  return (
    <div>
      <h1>Token Usage</h1>
    </div>
  );
}
