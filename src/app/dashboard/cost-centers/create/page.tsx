import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Cost Center - TokenTRA",
  description: "Create a new cost center",
};

export default function CreateCostCenterPage() {
  return (
    <div>
      <h1>Create Cost Center</h1>
    </div>
  );
}
