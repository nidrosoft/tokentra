import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Alert - TokenTRA",
  description: "Create a new alert rule",
};

export default function CreateAlertPage() {
  return (
    <div>
      <h1>Create Alert</h1>
    </div>
  );
}
