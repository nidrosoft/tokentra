import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Alert History - TokenTRA",
  description: "View past alert events",
};

export default function AlertHistoryPage() {
  return (
    <div>
      <h1>Alert History</h1>
    </div>
  );
}
