import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Create Team - TokenTRA",
  description: "Create a new team",
};

export default function CreateTeamPage() {
  return (
    <div>
      <h1>Create Team</h1>
    </div>
  );
}
