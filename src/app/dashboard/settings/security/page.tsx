import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Security - TokenTRA",
  description: "Manage security settings",
};

export default function SecuritySettingsPage() {
  return (
    <div>
      <h1>Security Settings</h1>
    </div>
  );
}
