import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "API Settings - TokenTRA",
  description: "Manage API configuration",
};

export default function ApiSettingsPage() {
  return (
    <div>
      <h1>API Settings</h1>
    </div>
  );
}
